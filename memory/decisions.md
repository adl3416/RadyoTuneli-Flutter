# Decisions

- Keep this context system small and cheap to read.
- Use `AGENTS.md` as the first entry point for future Codex chats.
- Use `memory/` files only when they match the current task.
- Preserve existing app behavior unless the user explicitly requests a change.
- Keep documentation updates separate from app code changes.
- Prefer existing Flutter and project patterns over new abstractions.
